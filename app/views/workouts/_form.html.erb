
<%= form_for(@workout, :class => 'form') do |f| %>
  <% if @workout.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@workout.errors.count, "error") %> prohibited this workout from being saved:</h2>

      <ul>
      <% @workout.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :workout_name, "Name your Workout" %><br>
    <%= f.text_field :workout_name %>
  </div>

  <div class="field">
    <%= link_to 'Add an activity to your workout', new_activity_path %>
    <!-- render activties/form -->
  </div>

    <!-- TODO add in activity form fields to workout form
    use debugging to fix up-->

  <div class="field">
    <%= f.label :workout_id, "WorkoutID" %><br>
    <%= f.text_field :workout_id, Workout.all, :id, :workout_name %>
  </div>
  <div class="field">
    <%= f.label :activity_id %><br>
    <%= f.text_field :activity_id, Activity.all, :id, :title %>
  </div>

  <div class="field">
    <%= f.label :notes %><br>
    <%= f.text_area :notes, :rows => '10' %>
  </div>
  <div class="actions">
  <%= f.submit :class => 'btn btn-default' %>
  </div>
<% end %>
