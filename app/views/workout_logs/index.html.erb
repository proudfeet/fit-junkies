<p id="notice"><%= notice %></p>

<h1>Listing Workout Logs</h1>

<table>
  <thead>
    <tr>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @workout_logs.each do |workout_log| %>
      <tr>
        <td><%= link_to 'Show', workout_log %></td>
        <td><%= link_to 'Edit', edit_workout_log_path(workout_log) %></td>
        <td><%= link_to 'Destroy', workout_log, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Workout log', new_workout_log_path %>
